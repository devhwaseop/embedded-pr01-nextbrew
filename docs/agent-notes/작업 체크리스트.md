# NextBrew 작업 체크리스트

> **에이전트(Claude Code)가 작성·갱신하는 작업용 목록입니다. 보고서·발표자료가 아니며 제출물에 옮기지 않습니다.**
> 사용자가 빠뜨리지 않도록 「할 일」과 「이 세션에서 한 일」을 모아 둡니다. 근거 링크는 [참고 출처 목록](참고%20출처%20목록.md).

## 1. 사용자가 할 일

### 배포·로그인 준비 (프로토타입 9/27 전)

- [ ] **Firebase 프로젝트 만들기** (GitHub와 같은 구글 계정) — 공식 안내: [웹 앱에 Firebase 추가](https://firebase.google.com/docs/web/setup)
  1. [Firebase 콘솔](https://console.firebase.google.com)에서 프로젝트 추가
  2. 프로젝트에 **웹 앱** 등록 → 화면에 나오는 `firebaseConfig` 값을 에이전트에게 전달
     (이 값은 공개돼도 되는 값입니다 — 데이터는 보안 규칙이 지킵니다)
  3. Authentication → 로그인 방법 → **Google** 사용 설정 — [안내](https://firebase.google.com/docs/auth/web/google-signin)
  4. Authentication → 설정 → **승인된 도메인**에 `devhwaseop.github.io` 추가
  5. Firestore Database 만들기 — [안내](https://firebase.google.com/docs/firestore/quickstart)
  6. Firestore → 규칙 탭에 저장소의 `firestore.rules` 내용을 붙여넣고 게시 — 9/24 완료(에이전트 확인: 계정에 쓰고 읽힘, 다른 uid 는 permission-denied)
  - 9/24 확인: 1·2·3·5는 됨(Google 로그인 주소 발급, Firestore 데이터베이스 있음). 4(승인된 도메인)도 9/24 완료 — 사용자 화면: `devhwaseop.github.io`(Custom)
  - 9/24 폰 설치형 웹앱 로그인: 도메인 추가 전에 설치·시도했을 때 `auth/unauthorized-domain`, 지금은 됨(사용자 확인). 원인(설치 시점 상태 vs 도메인 반영 지연)은 가리지 못함
  - 참고: 설치형 웹앱(standalone)에서 팝업 로그인이 불안정한 사례가 알려져 있음(polymerfire #269). 다시 막히면 Firebase 문서 「redirect-best-practices」 5번(팝업 없이 Google 로그인 → signInWithCredential)을 검토
- [x] **저장소 공개 전환** — 9/24 완료(비로그인 API 200). 전환 전 개인정보 점검은 9/24 대화
- [x] 저장소 Settings → Pages → Source를 **GitHub Actions**로 설정 — 9/24 완료(https://devhwaseop.github.io/embedded-pr01-nextbrew/ 200, 커밋 6e901a0)
  - Pages를 켜기 전에 `main`에 푸시하면 배포 작업이 실패로 표시됩니다(무해)
  - 9/24 첫 코드 푸시(098144e)에서 그대로 실패 확인: `configure-pages` 「Get Pages site failed … Not Found」. 비로그인 API 조회가 404 → 저장소가 비공개. 공개 전환(또는 비공개 Pages가 되는 요금제) + Source 설정 뒤 [Re-run jobs]
  - 공개 전 확인거리: 9/23 init 커밋의 발표 파일 이름에 학번·이름이 들어 있음(`docs/V1/`). 앱 코드·에이전트 노트에서는 이메일·uid 없음(9/24 grep)

### 나중에 확인할 것

- [ ] Firebase·Supabase 상세 비교 (직접 조사하기로 함)
- [ ] 원두 등록 항목이 SCA 외재적 평가 항목과 맞는지 확인
- [ ] 원두량 조정(스케일링) 원문 확인 — 배리스타 허슬 유료 글 결제 여부 결정 포함
- [ ] SCA 양식 출처 표기 방식 — 교수님께 확인 권장
- [ ] 자료 조사에 AI를 쓴 것이 괜찮은지 — 교수님께 확인 권장
- [ ] 설문 가정안을 실사용해 보고 판단
- [ ] 디자인 개선 (디자인 플랫폼으로 바탕을 만든 뒤 에이전트가 코드 수정)
- [x] AI 공유 묶음의 형식·데이터 결정 — 9/24 결정(아래 2절). 실사용하며 담을 항목 조정
- [ ] 초기 목록(드리퍼·필터·드립 방법) 보강 조사
- [ ] 개인 규칙 저장소 관련 제안 2건 — 다른 세션에서 처리 (내용은 9/23 대화 원문)

### 답이 필요한 것

- [ ] 로그인 전에 쌓인 로그 95줄은 이 기기(내장 브라우저)에만 있음 — 설정 [이 기기 기록을 계정으로 옮기기]로 계정에도 남길지
- [ ] Figma 연결 여부 — 교육용 플랜 인증(학교 이메일) 후 원격 MCP 서버 연결할지 (조사 결과 9/24 대화)
- [ ] 터치 44px — 9/24 적용(「일단 해 보고」 결정). 실기기에서 누르기 편한지·화면이 너무 길어지지 않는지 확인 후 조정
- [ ] Figma 좌석 — 교육용 팀에 관리자로 있으나 좌석이 View(9/24 whoami 두 번). 관리자 → 사람 → 좌석 유형을 Full 로 바꿔야 캔버스 쓰기 가능(결제 화면이 나오면 멈춤)
  - 9/24 오후 whoami 세 번째도 View. 사용자가 본 「편집 액세스 권한: 전체」 창은 파일·폴더 권한이라 좌석과 다른 설정. 교육용 팀도 관리자가 View → Dev·Full 로 바꿀 수 있고 무료(Figma 커뮤니티 지원 답변 8/3~4). 사용자 화면의 멤버 목록에는 좌석 유형 열이 보이지 않음 → 줄 끝 「›」 상세에서 찾아보기로 안내
- [ ] UI/UX 통일 목록·버튼·칩 경계 대비(1.32:1) — 디자인 작업 때 결정(9/24 대화의 표)
- [ ] 화면 단어·디자인 — 사용자가 정해 옴(에이전트 추천은 9/24 대화)
- [ ] **평가 대기(9/24 적용)** — 언스페셜티 후보 1~7·9. 써 보고 뺄지 정함. 특히 다음 추출 제안의 맛 대응(`app/js/core/compass.js`)과 「강배전은 쓴맛을 한 단계 낮춰 봄」은 에이전트가 정한 가정
- 9/24 해결: 준비 화면 「뜨거운 물」 칸 → 원두량·뜨거운 물·비율 세 칸을 다 조정하는 방식으로 정리(사용자 요청)
- [ ] 에이전트 문서(`docs/agent-notes/`) 위치 — 저장소에 둘지, 옮길지, 뺄지(9/24 대화에 방향 제시)
- 9/24 해결: 백업 설명 모양(B안: 켜짐·꺼짐에 따라 설명이 바뀜), 언스페셜티 후보 선택(8 수율 계산·10 일시정지만 제외)
- 9/25 해결: AI 답을 앱으로 가져오기 — 사용자 결정 「둘 다」로 구현(위 표)
- 9/25 해결: 레시피 자리 = 원두 탭 [원두 | 레시피] 전환(A안)
- 9/26 해결: 분쇄 측정 올리기 — CSV 주 경로 + 사진은 앱 안 글자 인식(Tesseract.js, «세밀하게 튜닝») + 줄인 사진은 계정 저장일 때만 보관(아래 결정 표)
- 9/26 해결: 원두 탭 네 칸 전환 = A안(짧은 옆 밀기 + 겹침). B안은 사용자가 뺌. [ ] 폰에서 보고 확정
- 9/26 해결: 「실제」 → 「그라인더 표시값 / 영점 반영값」 으로 통일(사용자 결정)
- [ ] 로스터리 설명 페이지 캡처 올리기(계획) — 사용자 9/26 방향 제안 «OCR 대신 레시피처럼 수동 + AI 로 통일». 에이전트 의견: 동의(표기 맞추기까지 AI 가 함). 구현 여부는 사용자 결정 — 설계는 9/26 대화
- [ ] AI 공유에서 「선택 안 함」 말고도 비어 있는 칸 — 「내 표현 —」(안 고름)·「잡미 없음」(안 고름)을 뺄지(9/26 질문)
- [ ] 아이폰 XS 홈 화면 앱에서 애니메이션이 안 보인 원인 — iOS 버전(18 미만이면 View Transitions 없음)·「동작 줄이기」 켜짐 여부 확인 필요. 두 경우 모두 대신 움직임을 넣음(9/26)
- [ ] 레시피 화면 프롬프트를 공유 화면과 같은 코드 상자 모양으로 바꿀지 — 9/25 질문을 사용자가 못 알아봄 → 다시 설명함, 답 대기
- [ ] **평가 대기(9/25 적용)** — 얼음이 모자라면 가수로 채운다는 원칙, 가수 추천의 «계획 비율», 배전도 5단계(극강배전 칸 없음), 소비기한 역산 처음 값 12개월, 남은 원두 알림 10g, 원 안 글자 크기(원 폭 비례 — 초 27%·저울 값 11%), 끌어 넘기기 기준(폭 25%·0.4px/ms)·가장자리 폭, 전환 시간(탭 280~380ms·앞뒤 300ms·펼치기 320ms)
- [ ] 아래 탭 원두 아이콘 — 레시피가 원두 탭으로 들어와 원두 아이콘 그대로 둘지, 원두+레시피를 뜻하는 모양으로 바꿀지(9/25 질문, 답 없음)
- [ ] 방치 확인 기준값(목표 초과 65초에 묻고, 5분 무응답이면 기록 없이 끝냄) — 써 보고 조정. `app/js/core/timer.js` `STILL_HERE`
- 9/24 해결: 1차 푸어 역할 문장(비움 + 직접 쓰기), 연타 방지(탭 + 되돌리기 + 밀어서 취소), npm 파일 네 개(휴지통으로 옮김)

### 아직 확인 못 한 것 (실기기·배포 뒤에 확인)

- [ ] 오프라인 캐시(서비스 워커) — 개발용 내장 브라우저가 서비스 워커를 막아 확인 불가. 배포 뒤 폰에서 확인
- [ ] 화면 꺼짐 방지 — 내장 브라우저가 권한을 거절(`NotAllowedError`). 실제 폰에서 확인
- [ ] 홈 화면 설치(PWA)
- [x] Firebase 로그인·계정 저장·규칙 — 9/24 확인(두 기기 사이 동기화는 폰에서 확인)
- [ ] AI 공유의 [공유하기](운영체제 공유창) — 9/25 사용자가 폰에서 `Permission denied` 를 봄 → 원인(크롬 허용 목록에 .md·.json 없음)을 헤드리스 크롬 실제 클릭으로 재현, .txt 로 보내게 고침(.txt 는 막히지 않고 공유창 대기까지 확인). 폰 공유창에서 AI 앱으로 넘어가는지는 폰에서 확인
- [ ] 밀어서 취소를 손가락으로 — 마우스 끌기로만 확인함
- [ ] 분쇄 측정 사진 인식을 폰에서 — 첫 엔진 받기(약 4.5MB)·폰 속도·폰 캡처(1080px 이상)로 확인. 헤드리스 크롬 실측은 0.5~1초
- [ ] 계정에 보관한 측정 사진 크기(가로 720 JPEG, 300KB 이하로 줄임) — 로그인한 상태로 한 번 올려 `grind.measureImport` 로그의 photoBytes 확인

### 커밋 시점 (에이전트 제안 — 커밋·푸시는 사용자가 함)

| 시점 | 무엇이 끝났을 때 |
|---|---|
| ① | 뼈대 + 핵심 로직 + 테스트 통과 |
| ② | 화면 흐름(준비 → 타이머 → 결과 → 설문 → 기록) 동작 |
| ③ | Firebase 로그인·동기화 연결 |
| ④ | 실기기에서 실제로 한 번 추출 후 수정 |
| (9/24 제안) | 9/23 init 커밋(문서만) 뒤로 앱 코드가 아직 한 번도 커밋되지 않은 상태라, 9/24 기능 보강까지 한 번에 묶기 좋은 시점. 명령 예시는 9/24 대화에서 줌 |

## 2. 확정된 결정

| 항목 | 결정 | 결정일 |
|---|---|---|
| 기술 스택 | 빌드 없는 HTML/CSS/JS(ES 모듈), `app/`만 GitHub Actions로 Pages 배포 | 9/23 |
| 저장 | Firebase(Firestore + Google 로그인), 오프라인 저장 후 자동 동기화, JSON 내보내기/가져오기는 백업용 | 9/23 |
| 로그인 | 팝업 방식(리디렉트 방식은 최신 브라우저에서 막힘) | 9/23 |
| 사용자 범위 | 여러 사용자가 쓸 수 있게 만들되 실사용은 본인 중심 | 9/23 |
| 기기 | 폰 중심 + PC에서도 사용 | 9/23 |
| 타이머 오른쪽 위 | 전체 경과 시간 | 9/23 |
| 단계 이름 | 뜸 들이기 / N차 푸어 / 종료 + 레시피별 회색 설명 한 줄 | 9/23 |
| 초과 | 0초가 지나면 `+N초`로 초과를 표시하며 버튼을 기다림 | 9/23 |
| 일시정지 | 없앰 → **취소**(재확인). 취소하면 기록은 저장하지 않고 로그에만 남김 | 9/23 |
| 종료 | 마지막 단계에서 [다음]이 [종료]로 바뀜. 결과 화면에서 「다 빠짐 / 물이 남은 채 종료」 선택 | 9/23 |
| 비교 | 같은 레시피의 직전 기록(드리퍼 무관), 없으면 같은 원두의 직전 기록과 **모든 항목을 양쪽에 나란히 1:1 비교**. 다른 줄 강조, 시간은 ±2초 이내 무시, 한쪽이 설문 전이면 맛은 다름으로 세지 않음 | 9/23 |
| 붓는 구간 강조 | 레시피의 「한 번 붓는 시간」(쿠라스 10초) 동안 목표 g을 파스텔 빨강으로 은은하게 깜박임. 동작 줄이기 설정이면 색만 | 9/23 |
| 레시피 | 원두량 기준 비율로 계산(물·얼음·단계 목표), 시각은 고정. 기준 원두량으로 먼저 내리고 바꿔 가며 비교 | 9/23 |
| 핫/아이스 | 둘 다, 아이스 중심. 얼음 g·가수 g 입력, 추천값을 기본값으로 | 9/23 |
| 온도 | 가열을 끝낸 시점의 온도 | 9/23 |
| 분쇄 | 클릭 표기 `110(-3)` = 다이얼 110, 영점 −3 → 실제 107. 영점과 값 모두 변경 가능 | 9/23 |
| 드리퍼 | 보유: Hario V60 02, V60 MUGEN 02 | 9/23 |
| 프리셋 | 쿠라스 재팬 아이스(V60) | 9/23 |
| 설문 | 강도 슬라이더(선택 안 함 가능) + 전체 만족도 + 종류 칩 + 항목 메모. 가정안으로 써 보고 판단 | 9/23 |
| 잡미 표기 | 영문 통상어 + 괄호 한국어 묘사, 예: `Chalky (가루 섞인 느낌)` | 9/23 |
| 원두 노트 | 직접 입력 + 같은 산지·가공의 이전 등록에서 추천 | 9/23 |
| 로그 | 사건이 생길 때만 시각을 찍어 시간순 기록, 설명은 코드 주석. 설정에서 켜면 내보내기에 포함 | 9/23 |
| V2 범위 | 준비 → 타이머 → 결과 → 설문 → 기록 + 로그인·동기화 + JSON 백업 + AI 공유 버튼·화면(형식은 나중에) | 9/23 |
| 보류 | 앱 URL 공유 / 산지 데이터 외부 연동 / 커스텀 레시피 편집기 / 고정 레시피 (µm 보조값은 9/24 보조 성격으로 도입) | 9/23 |
| 붓기 안내 | 붓는 구간에 「지금 약 Ng · 초당 Ng · N초」(고르게 부을 때의 기준선), 지나면 「Ng 붓기 끝 · 기다리는 중」 | 9/24 |
| 누른 시각 판정 | 레시피 시각과 비교해 「N초 빠름·N초 느림·적절(±2초)」. 타이머 막대와 결과·상세 단계표에 표시 | 9/24 |
| 연타 방지 | 뗄 때 실행, 누른 채 밖으로 밀면 취소. 넘긴 뒤 5초 [되돌리기], [종료]는 3초 되돌리기 뒤 저장 | 9/24 |
| 방치 확인 | 넷플릭스 방식: 목표를 레시피 절반(최소 60초) 넘기도록 안 누르면 「아직 추출 중인가요?」, 5분 무응답이면 기록 없이 끝냄(로그만) | 9/24 |
| 단계 설명 | 직접 쓴 문장 → 레시피 원문 → 일반 설명 → 비움. 준비 화면에서 직접 쓰기. 「10초 안에」는 레시피 전체 팁으로 이동 | 9/24 |
| 추천 붓는 방법 | 레시피마다 원문 근거가 있을 때만 표시. 쿠라스는 원문에 없음 | 9/24 |
| 서버 무게 | 서버(이름·자체 무게) 등록, 결과 화면 스위치로 켜고 끔, 서버 뺀 무게 계산 | 9/24 |
| 설문 순서 | 쓴맛 → 바디감 → 단맛 → 산미(개인 기준) | 9/24 |
| AI 공유 | 이번 + 직전(무관) + 레시피·원두가 같은 최근 기록을 한 파일로. MD/JSON 선택, 기본값은 설정(처음 MD). 「묶는 중」 표시 뒤 파일 저장·공유창 | 9/24 |
| 로그 | 테스트 데이터를 지워도 로그는 지우지 않음 | 9/24 |
| 화면 단어 | 사용자가 정한 단어로 교체, 핵심 단어는 진하게·부가 설명은 옅게(`app/js/core/words.js` 한 곳). 종료 상태는 key(drained·cutoff)로 저장 | 9/24 |
| 선택 설명 | 설명이 있는 선택지는 «고른 것»의 설명을 아래에 보이고, 바꾸면 설명도 바뀜(종료 상태·린싱·AI 공유 형식) | 9/24 |
| 줄바꿈 | 한국어는 어절 단위(`word-break: keep-all`), 긴 덩어리만 그 안에서 끊음, 설명문은 `text-wrap: pretty`. 출처는 작은 글씨 | 9/24 |
| 그라인더 변환 | 보조 성격: 설정에 변환 사이트 링크 두 곳 + 준비 화면 「참고 µm」(선택, 직접 입력, 앱은 계산 안 함). 9/23 「µm 보조값 화면」 보류를 이것으로 해제 | 9/24 |
| UI 시안 | 앱 화면 디자인은 코드 범주(사용자 판단). 화면 흐름도를 발표자료에 넣을 때는 직접 그림 | 9/24 |
| 계정 옮기기 배너 | 계정 모드에서 이 기기에만 있는 기록이 있으면 「이 기기에만 있는 기록 N건 · [계정으로 옮기기]」. 옮기기는 로그를 (시각, 사건)으로 걸러 두 번 옮겨도 한 벌 | 9/24 |
| 글자 대비 | 옅은 글자 `--muted` = `#766e69`(4.68~4.99:1), 단계 설명도 같은 토큰 | 9/24 |
| 누름 영역 | 버튼·칩·입력·슬라이더·따로 떨어진 글자 링크 44px 이상(문장 안 링크는 예외) — 실기기 확인 뒤 조정 | 9/24 |
| 로그인 판정 | 계정 데이터를 동시에 읽음, 직전 로그인 계정을 기억해 확인 중에는 「계정 확인 중…」 표시 | 9/24 |
| 로그인 버튼 | 설정은 Google 공식 가이드 모양의 「Google 계정으로 로그인」 버튼 하나(배너 링크 없음). 홈 배너 링크는 유지 | 9/24 |
| Debug Log | 이름 「Debug Log」, 켜고 끄는 스위치 | 9/24 |
| AI 공유 담을 기록 | 「비교 기록 함께 / 이 기록만」, 공유 화면에서 고르고 기본값은 설정. 이 기록만이면 MD 에 「담지 않음」 | 9/24 |
| 분쇄 측정 링크 | 설정 그라인더 칸에 언스페셜티 분쇄도 가이드(사진으로 내 분쇄 측정)·개발기 링크 추가. 추정 사이트 두 곳과 성격이 달라(측정 vs 추정) 줄을 나눔 | 9/24 |
| 언스페셜티 후보 | 1 참고 µm 측정 링크·표준편차 · 2 누적 물량 그래프(계획·실제·두 기록 비교) · 3 단계 시간표 · 4 타이머 전체 진행 막대 · 5 맛 → 다음 추출 제안(커피 컴퍼스) · 6 클릭당 µm(설정값 또는 기록으로 추정) · 7 원두 배전도 · 9 레시피 태그·드리퍼 필터(드리퍼가 둘 이상일 때만 보임). 8 수율 계산·10 일시정지는 제외. 적용 후 평가 | 9/24 |
| 그라인더 변환(갱신) | 6번으로 앱이 «클릭 수»는 계산한다 — 외부 변환 데이터가 아니라 사용자가 적은 클릭당 µm 나 자기 기록의 참고 µm 로만. µm 자체를 앱이 만들지는 않음 | 9/24 |
| 개발기 칼럼 | 에이전트 확인용으로 받은 것 → 앱 링크에서 뺌(사용자 정정) | 9/24 |
| 스위치 모양 | 이름 한 줄 + 아래 옅은 설명, 스위치 오른쪽. Debug Log·보조 설명은 켜짐/꺼짐에 따라 설명이 바뀜(B안). 결과 화면 서버 스위치도 같은 모양 | 9/24 |
| 보조 설명 | 항목 이름 옆 옅은 설명을 한 모양(term)으로 통일(분쇄 크기·참고 µm·린싱·물 온도 등), 설정 「화면 → 보조 설명」으로 켜고 끔(기본 켬) | 9/24 |
| 아래 탭·단어 | 탭은 아이콘만(Lucide, ISC) + 화면 읽기용 이름. 「새 추출 준비」 → 「추출하기」. 「봉투에 적힌 노트」 → 「로스터리 표기 노트」. 입력칸 제목은 진하게 | 9/24 |
| 원두와 물의 비율 | 「1:15」 표기를 보조 설명이 아닌 독립 항목으로(사용자 정정 9/24): 조건 목록·비교표·AI 공유 파일·레시피 요약 줄·이번 계획 줄. 준비 화면에서는 **원두량(g)·뜨거운 물(g)·비율(1:x) 세 칸을 다 조정**(사용자 요청 9/24): 비율을 바꾸면 물 = 원두량 × 비율, 물을 바꾸면 비율이 따라감, 원두량을 바꾸면 비율 유지·물이 따라감. 레시피와 다르면 [레시피 비율로]. 아이스면 얼음 포함 비율을 따로 적음 | 9/24 |
| 링크 버튼 | 밑줄 글자 링크로 된 버튼(＋ 원두 등록·사진으로 분쇄 재기·그라인더 설정·두 기록 비교)을 보조 버튼 모양으로 통일. 앱 안 이동 「›」, 바깥 사이트 「↗」(화면 읽기용 「새 창」) | 9/24 |
| 단계 설명 줄바꿈 | 타이머 단계 설명은 문장마다 줄을 바꾼다. 보조 설명(term-sub)은 줄 끝에 안 들어가면 통째로 다음 줄로 | 9/24 |
| 레시피 가져오기 | 앱의 프롬프트를 AI 에 붙여 넣어 JSON 을 받고, 붙여넣기·파일로 올림 → 앱이 읽고(특수문자·단위·m:ss 고쳐 읽음) 항목별 폼에 채움 → 칸을 고칠 때마다 다시 검사 → 미리 보기 → 저장. 직접 입력·저장한 레시피 수정·삭제도 같은 화면(`#/recipes`). 비율·단계 % 는 앱이 계산. 프롬프트 예시는 쿠라스 프리셋에서 만들고 테스트가 «예시 → 검사 → 변환 = 프리셋»을 묶음. 앱이 못 잡는 것(AI 가 숫자를 잘못 옮김)은 미리 보기에서 원문과 대조하라고 안내 | 9/24 |
| 화면 채우기 | `replaceChildren` 이 null 을 「null」 글자로 넣어 세 번 화면에 찍힘(원두 노트·타이머·준비 화면 — 마지막은 설문한 기록이 없을 때만 나와 캡처용 시드로는 못 봄) → `ui/dom.js` 의 `fill()` 하나로 모으고, 직접 쓰면 `tests/app-files.test.mjs` 가 실패(위반을 넣어 실패하는 것까지 확인) | 9/24 |
| 로그인 안내 | 「로그인하지 않아 이 기기에만 저장 중입니다.」 바로 아래에 구글 로그인 버튼(설정으로 보내던 「로그인」 글자 링크를 뺌). 설정 화면은 버튼이 따로 있어 배너에는 넣지 않음 | 9/25 |
| 분쇄 표시 | 그라인더에 보이는 값을 넣으면 옆에 「실제 125 · 영점 −3 반영」. 「표기 128(-3)」과 단위 「클릭」은 뺌(단위는 항목 이름에 있음) | 9/25 |
| 얼음 무게가 다를 때 | 뜨거운 물은 그대로(추출을 정함), 얼음은 식히고 묽히는 몫이라 모자란 만큼 추출 뒤 가수로 채움. 많으면 최종 비율이 얼마나 연해지는지 보이고 덜어 내라고 안내. 기록에 넣은 얼음(`iceG`)과 추천 얼음(`iceTargetG`)을 함께 남기고, 결과 화면 가수의 처음 값 = 레시피 가수 + 모자란 얼음 | 9/25 |
| 추출 후 무게·가수 | 「서버 무게 재기」는 처음부터 켜짐(끄거나 비워 두면 안 잼). 가수 전·후 총무게(서버 포함 저울 값) → 가수 = 후 − 전 자동, 원액 무게 = 전 − 서버. 가수 아래에 가수 포함 비율과 «계획 비율까지 가수 몇 g 더». 계획 비율 = 계획 뜨거운 물 + 추천 얼음 + 레시피 가수 | 9/25 |
| 설문 저장 | 「저장 및 공유」 한 버튼 + 고르는 창 → [저장]·[공유] 두 버튼. [공유]도 먼저 저장하고 공유 화면으로 | 9/25 |
| 공유 오류 | 크롬은 `.md`·`.json` 파일 공유를 `share()` 에서 막고(`NotAllowedError: Permission denied`), `canShare()` 는 이를 모름 → 공유창으로는 같은 내용을 `…-md.txt`(text/plain)로 보냄. 파일 저장은 고른 형식 그대로. 헤드리스 크롬에서 실제 클릭으로 재현·확인 | 9/25 |
| 추출 보기 | 누르면 「추출 숨김」으로 바뀜(`aria-expanded` 함께) | 9/25 |
| 원두 등록 | 이름 자동(국가·지역·생산자·품종·가공방식, 스위치로 직접 적기 — 9/25 전 원두는 이름을 지키려고 «직접»으로 염). 「가공 방식」 → 「가공방식」. 배전도 5단계 슬라이더(약·중약·중·중강·강, 다크 = 강배전). 구매 무게(g·kg·oz·lb) → 기록 원두량을 빼 남은 원두 추정, 10g 이하면 홈·원두 목록에 알림과 [소모로 바꾸기]. 소모한 원두는 목록에서 흐리게, 준비 화면 목록에서 뺌. 제조일(로스팅일)·개봉일, 소비기한만 있으면 기간(처음 12개월)을 빼서 «추정» 제조일 | 9/25 |
| 타이머 시작 | 준비 화면 [다음] → 타이머 화면 대기(0:00) → [시작]부터 잰다. 대기 화면 [뒤로]는 묻지 않고 준비 화면으로(입력값 유지). `brew.start` 는 [시작]에서 남기고 `readySec` 를 더함 | 9/25 |
| 원 안의 저울 값 | 붓는 동안 「지금 12g」을 원 안에 크게(1.7rem), 아래 막대에는 목표 g·초당 g·남은 붓기 초 | 9/25 |
| 종료 기준 | 물줄기가 방울로 바뀌면 [종료]를 누르고 드리퍼를 뗌 = 「모두 내려감」, 물줄기가 이어지거나 물이 고인 채로 떼면 「강제 종료」. 레시피 시각(쿠라스 원문 «Approx. 2:10»)은 기다릴 시각이 아니라 비교할 목표 | 9/25 |
| 날짜 칸 | 숫자 칸과 같은 [−] 날짜 [+] 모양(하루씩, 가운데를 누르면 기기 달력), 아래에 「오늘 / N일 전」과 작은 [비우기]. 새 원두는 제조일·개봉일을 오늘로 채움, 이미 등록한 원두의 빈 날짜는 채우지 않음(다른 칸만 고쳐 저장해도 날짜가 생기지 않게 — −/+ 는 오늘부터). 소비기한 칸의 처음 값 = 오늘 + 기간 | 9/25 |
| 원두 상태 버튼 | 수정 화면 「상태」 자리의 [사용 중][소모] 칩 → 눈에 띄지 않는 [소모로 바꾸기]·[사용 중으로 복구](누르면 바로 저장, `bean.status` via form). 홈·원두 목록의 10g 이하 알림과 [소모로 바꾸기]는 그대로(사용자 확인 9/25 「앞에 넣은 기능 빼면 안돼」) | 9/25 |
| 글자 크기·여백 | 역할별 변수로 통일: 글자 여섯 단계(제목 1.4 · 칸 제목 1.05 · 본문 1 · 입력칸 이름 0.9 · 설명 0.85 · 부가 설명 0.78rem), 여백 세 단계(4·8·12px)와 상자 안쪽 10·12px. 제목 아래 여백은 칸 간격이 맡음. 타이머의 멀리서 보는 숫자·기호·그래프 눈금·구글 버튼만 예외 — `tests/app-files.test.mjs` 가 예외 목록 밖의 직접 적은 크기를 막음(위반을 넣어 실패하는 것까지 확인). 슬라이더 색을 앱 색으로, 배전도 슬라이더 배치를 설문과 같게, 칸 안 스위치 이름은 보통 굵기 | 9/25 |
| AI 공유 프롬프트 | 공유 화면에 「보내는 순서」 3단계와 프롬프트 코드 상자(오른쪽 위 복사 아이콘 + [프롬프트 복사]). 프롬프트는 고칠 수 있고 칸을 벗어나면 설정 `sharePrompt` 에 저장, [기본으로 되돌리기]. [공유하기]는 파일만 보냄. 기본 질문 = 다음 추출 조정 제안(사용자 결정). 역할 이름은 파일 표기에서 가져오고 테스트가 묶음. 로그 `share.promptCopy`·`share.promptEdit` | 9/25 |
| 슬라이더 정렬 | 눈금 단어를 단계 수(--n)만큼 같은 폭 칸의 가운데에 두고, 슬라이더를 양옆 반 칸씩 줄여 손잡이 가운데가 칸 가운데에 오게 함(손잡이 24px 로 직접 그림, 왼쪽은 앱 색으로 채움). 설문 5개·배전도 1개 모두 — 실측 어긋남 최대 0.02px | 9/25 |
| ± 칸·칸 제목 | ± 칸 가운데 정렬, 칸 사이 12px, 입력칸 140px. 칸(카드) 제목 1.05 → 1.2rem(왼쪽 정렬 그대로) | 9/25 |
| 기록이 없을 때 | 기록·결과·설문·공유 화면 공통(`brewNotFound`). 로그인 전: 「기록이 기기에 없습니다. / 계정에 저장된 기록이라면 로그인 후 확인할 수 있습니다.」 + 구글 로그인 버튼을 홈과 같은 칸에. 로그인 뒤: 「다른 사람의 기록이므로 접근할 수 없습니다.」 + 초록 「계정에 저장 중 · 이메일」(버튼 없음). 계정 확인 중에는 「계정 확인 중…」 | 9/25 |
| 설정 로그인 | 설정 「저장 위치」도 홈과 같이 안내 칸 안에 로그인 버튼(9/24 에는 칸 밖에 따로 둠) | 9/25 |
| AI 제안 가져오기 | 흐름 「둘 다」(사용자 결정): 처음 프롬프트에 형식(`nextbrew-advice` v1) + 결론이 나면 AI 가 한 번만 묻기, 기록 화면 [결과 받기 프롬프트 복사]로 다시 요청. 값은 «다음 추출에 쓸 값»(다이얼·원두 g·물 g·℃ — 차이가 아니라 바꾼 뒤 값). 기록 화면 「AI 제안」에 붙여넣기·파일 → 너그럽게 읽기(`core/looseJson.js`, 레시피와 공용) → 검사(다른 기록의 답·범위·크게 바뀐 값) → 미리 보기 → 기록에 저장(`aiAdvice`). 준비 화면 「AI 제안」 카드의 [AI 제안대로 맞추기](다이얼은 같은 그라인더일 때만). 공유 MD 에 기록 ID 추가 | 9/25 |
| 아래 탭 슬라이드 | 탭 순서대로 화면을 늘어놓고 밀어 넘김(사이 탭 화면도 지나감). 1칸 280·2칸 340·3칸 380ms(한 칸당 점점 빨라짐), Material 표준 곡선, 동작 줄이기면 끔, 탭 막대는 그대로. Material 은 아래 탭에 fade through 를 권하지만 방향 슬라이드는 사용자 결정 | 9/25 |
| 공유창 파일 이름 | `nextbrew-20260925-1623(md).txt` · `(json).txt`(사용자 요청 9/25 — 괄호 표기). 크롬 공유 허용 확인(실제 클릭, 공유창 대기) | 9/25 |
| 레시피 자리 | 원두 탭 위 [원두 \| 레시피] 전환(A안, 사용자 결정 9/25) — 아이콘 bean·book-open(Lucide), 칸을 옮기면 한 칸 슬라이드, 탭 막대는 원두. 설정의 레시피 칸은 뺌. 아래 탭 아이콘은 원두 그대로(따로 정하지 않음) | 9/25 |
| 시각 요소 다섯 | 사용자 결정 「모두 적용」: ① 목록 줄 → 상세 커지기(container transform — View Transitions, 전환 중만 상세를 화면 높이로 잘라 줄이 화면 크기로 커지게) ② 준비 → 타이머 → 결과 → 설문 앞뒤(shared axis Z) ③ 탭 표시 막대 이동(끌 때는 손가락을 따라감) ④ 누름 반응(눌리는 동안 0.97배) ⑤ 원 안 초 숫자 바뀜·저울 값 0.1초 갱신. 동작 줄이기면 모두 끔 | 9/25 |
| 끌어 넘기기 | 아래 탭 네 첫 화면에서 가로로 끌면 옆 탭이 따라 보이고, 폭 25%·0.4px/ms 넘으면 넘김. 가장자리 제외(사용자 지시): 좌우 40px(안드로이드 뒤로 가기 30dp×최대 1.33, AOSP) · 아래 48px+안전 영역(홈 제스처 48dp, 아이폰 홈 인디케이터) · 위 24px+안전 영역(알림창). 입력칸·슬라이더·탭 막대에서 시작하면 받지 않음, 끈 직후 클릭은 막음 | 9/25 |
| 타이머 | 원 전체 누름 = [시작]·[다음 푸어]·[종료](버튼과 같은 연타 방지, 물결·눌림, 밀면 「놓으면 취소」, 로그 via). [다음 푸어] 크게·넓게, 「초당 4g · 7초 안에」 = 「40g까지 붓기」 크기, 설명·「다음:」 크게. 스크롤·확대 막음(touch-action none · 안드로이드 viewport user-scalable=no · iOS gesturestart), 높이 dvh·원 min(80vw, 38dvh, 360px)·원 안 글자 원 크기 비례 — 360×640 ~ 768×1024 다섯 크기 모두 스크롤 없이 들어감(실측) | 9/25 |
| 방치 확인(9/26) | 마지막 단계가 아니면 «다음 단계 시간의 절반»이 지나도록 안 누르면 묻는다(쿠라스 55초·100초), 마지막 단계는 전처럼 레시피 절반(65초). 창에 [레시피 시각에 넘긴 걸로 고치기]·[아직 이 단계예요]·[기록 없이 끝내기] | 9/26 |
| 누른 시각 고치기 | 못 누른 것만이 아니라 모든 단계를 사후에(사용자 정정 9/26 — 앱은 늦게·일찍 누른 것을 모른다): 결과 화면 [누른 시각 고치기] — 시작 보정(초, 모든 시각 이동)과 단계별 끝 시각. 타이머 기록은 그대로, result.startShiftSec·stepFix 를 겹쳐 봄(timerOf) — 표·비교·그래프·AI 공유 모두 | 9/26 |
| 타이머 원 | 원 안: 큰 숫자 + 작은 「초」 / 「남음」 / 붓는 동안 빨간 알약 「7초 · 42g」 / 맨 아래 「100g까지」. 단계 이름은 원 밖 아래 크게, 원 밖 빨간 상자는 없앰, 원 min(84vw, 44dvh, 400px). 다섯 화면 크기 스크롤 0(실측) | 9/26 |
| 테이스팅 노트 | 「맛 설문」 → 「테이스팅 노트」(화면·AI 공유 글). 「선택 안 함」에서 가운데(보통)를 누르면 고른 것으로(pointerup·change). 같은 원두의 이전 기록에서 노트마다 가장 최근 인식 값을 채움(이 기록에만 저장) | 9/26 |
| 추출 후 칸 | 무게 칸 연동: 가수 전 + 가수 = 가수 후, 가수 후 + 추가 얼음 = 얼음 넣은 뒤 — 마지막에 고친 쪽이 원본, 칸은 사라지지 않음. 추가 얼음(아이스, 처음엔 꺼짐) — 안 적으면 AI 공유에 «기록 안 함(더 넣었을 수 있음, 양 모름)», 프롬프트에도 한 줄. 결과 「추가 입력」 | 9/26 |
| 다음 추출 제안 문구 | 「분쇄 N클릭 가늘게(그라인더 표시값 −N · 약 µm)」(9/26 용어 통일 전에는 「다이얼 숫자」), 클릭당 µm 를 모르면 「분쇄 조금 가늘게(약 µm)」 + 안내. 기록·준비 화면에 다음 다이얼 숫자 | 9/26 |
| 원두 탭 네 칸 | [원두 | 레시피 | 그라인더 | 서버](설정의 그라인더·서버를 옮김). 아이콘 위·이름 아래. 그라인더 아이콘은 사용자가 준 핸드밀 그림 느낌으로 직접 그림, 서버는 Lucide beaker | 9/26 |
| 분쇄 측정 올리기(9/26 결정) | [측정 결과 불러오기(사진·CSV)] 하나로 파일 종류를 나눈다. CSV = 주 경로(평균은 파일 이름, 그래프는 원 데이터로 직접). 사진 = 앱 안 글자 인식(Tesseract.js 7, kor+eng) → ① 「이 값이 맞나요?」 사진과 값 나란히·모든 칸 고침·확인 필요 칸 테두리 → ② 「Click N 은 어떤 값인가요?」 [영점 반영값]·[그라인더 표시값](각각 표시값 미리 보임). 줄인 사진(가로 720 JPEG)은 계정 저장일 때만 기록에 둠(이 기기 저장은 localStorage 한도 약 5MB). 인식 실패·오프라인이면 빈 칸으로 열어 직접 적음. AI 공유에는 측정 값 한 줄만(사진 빼고) | 9/26 |
| 사진 인식 튜닝(9/26 실측) | 카드 찾기(줄 어두움 + 카드 왼쪽 가장자리 띠, 파란 점 2% 넘으면 그래프로 보고 안 읽음) → 흑백·뒤집기·폭 1800(못 읽으면 2400 으로 다시)·이진화 끔 → 두 번 읽기(① 전체를 kor+eng 로, 「표준편차」 글자 위치로 칸 나누기 ② 숫자 단어를 글자 위치로 잘라 숫자·점만 허용해 다시) → 두 읽기가 같으면 믿음. 캡처 원본·1080·558px JPEG·419px JPEG 40·잘라 낸 사진 다섯 가지 **25/25 칸**(시작 14/25). 메모의 「g1」은 「81」로 읽혀 확인 창에서 고침 | 9/26 |
| 용어(9/26 결정) | 「그라인더 표시값」(그라인더에 보이는 숫자) / 「영점 반영값」(표시값 + 영점) — 준비 화면 분쇄 줄·조건 표·비교 표·제안 문구·AI 제안 형식 설명·측정 확인 창 | 9/26 |
| AI 공유의 「선택 안 함」(9/26 결정) | 항목째로 뺀다(MD 줄·JSON 키). 강도는 안 골랐어도 종류·메모가 있으면 그것만. 프롬프트에 «없는 항목은 고르지 않은 것 — 판단에 쓰지 말 것» 한 줄. 설정 JSON 내보내기는 형식을 맞추려고 그대로(null). 사용자 판단 기준: AI 공유 = 한 번의 상황 판단이라 편향 줄이기, 내보내기 = 데이터 보관·전체 기록 판단이라 형식 통일 | 9/26 |
| 여러 기록 공유 | 기록 목록에서 한 줄을 0.5초 길게 누르면 고르기 모드(체크 칸, 안드로이드 길게 누르기 메뉴 대신) → 아래 [취소]·[공유 (N)] → AI 공유 화면(형식·프롬프트 그대로, 「담을 기록」 칸 없음). 고른 것만 시간순, 가장 최근이 기준, 파일 `…-Nbrews.md`, 고른 기록용 기본 프롬프트(고친 글은 따로 저장) | 9/26 |
| 원두 등록(9/26) | 배전도 0.5~10(0.5 단위 숫자, 단어는 2칸씩 5단계 띠에서 정함 — 옛 원두는 단어 그대로 알림). 「다크 로스트」 안내·디카페인 설명은 설정 「보조 설명」을 따름. 로스터리 표기 노트 칩 오른쪽 위 동그란 ×(누르는 자리 32px). 디카페인 스위치 + 설명 4줄 + 추출 준비 보조(약 20µm 굵게 = 클릭당 µm 로 클릭 수, 원두 +0.5g 단추 — 누를 때만 바뀜). 로스터리 맛 지표(5점·10점 척도, 0.5 단위, 산미·단맛·바디·쓴맛·고소함·밸런스 + 직접, 척도를 바꾸면 비율대로 옮김). 가공방식 목록에 출처 「? (콩볶는사람들… · 인도네시아)」·「(이 원두)」 — 목록이 원두 값에서 만들어져 그 원두를 고치면 사라지므로 따로 지우기는 두지 않음 | 9/26 |
| 화면 전환(9/26) | 네 칸 = A안(칸 줄은 그대로, 내용만 28px 밀리며 겹침 — 나감 90ms·들어옴 70ms 뒤 210ms). 동작 줄이기면 끄지 않고 겹쳐 바꾸기(150ms, iOS 방식). View Transitions 없는 브라우저(iOS 17 이하)는 새 화면만 WAAPI 로 커지거나 작아지며 나타남 | 9/26 |
| 버튼 색 | 누를 수 있는 보조 버튼: 채움 #efe4d8 · 테두리 #9a8169(흰 칸 3.67:1 — WCAG 1.4.11) · 글자 10.9:1. 칩(고르는 칸)은 흰색 그대로 | 9/26 |
| 고정 안내 위치 | 칸 전체에 해당하는 고정 안내는 칸 제목 바로 아래, 고른 것에 따라 바뀌는 설명은 칩 바로 아래(설정 AI 공유·공유 화면). 사용자가 낸 두 안 중 「제목 아래 줄」을 칸 단위로 적용 — 근거 GOV.UK 라디오 안내문 위치 | 9/24 |

## 3. 이 세션에서 한 일 (시간순)

### 9/23
- 보고서·UI 스케치·참고 기록 3건(형식만)·저장소 상태를 읽고 이해한 내용과 V2 계획을 제안
- 저장소가 비공개인 것과 GitHub Pages 요금제 조건을 확인
- 사용자 답변을 받아 결정 사항을 확정(위 2절)
- 조사: 동기화 가능 여부, Firebase·Supabase 비교, 타이머 표시 근거, 아이스 프리셋, 원두량 조정, SCA CVA 설문 구조, 「가루맛」 표현, 산지별 노트 기준, 뜸 설명 근거, Firebase 설정·로그인 방식
- 확인용 문서 작성: 이 체크리스트, [참고 출처 목록](참고%20출처%20목록.md)
- 코딩: 빌드 없는 PWA를 `app/`에 구현
  - 핵심 로직(`app/js/core/`): 데이터 형식·레시피 비율 계산·타이머 엔진·직전 기록 비교·사건 로그·저장소·내보내기·노트 추천
  - 화면: 홈 · 준비 · 타이머 · 결과 · 설문 · 기록 목록/상세 · 원두 등록 · 설정 · AI 공유(자리만)
  - Firebase 연결 코드(`app/js/platform/firebase.js`) — 설정값이 없어 **아직 한 번도 실행되지 않음**
  - PWA 파일(매니페스트·서비스 워커·임시 아이콘), `firestore.rules`, 배포 워크플로(`.github/workflows/pages.yml`)
  - 테스트 16개(`node --test 'tests/*.test.mjs'`) 모두 통과
- 로컬 서버 + 내장 브라우저로 흐름 확인: 그라인더·원두 등록 → 준비(`110(-3) · 실제 107`) → 타이머 → 새로고침 뒤 이어받기 → 결과 보정 → 설문 → 상세 → 두 번째 추출 비교(「지난 추출보다 −46초」) → 취소(기록 안 남고 로그만)
- 고친 것: 경과 시간 반올림(0.7초가 0:01로 보임 → 내림), 레시피 변경 시 원두·도구 선택이 초기화되던 것, 사용자 입력이 HTML로 해석될 여지(innerHTML 제거), 한글 경로 테스트 실패
- Firebase 설정값을 `app/js/config.js`에 연결(애널리틱스는 켜지 않음) — SDK 로드·로그인 버튼 표시 확인
- 1:1 비교표(상세 화면)·붓는 구간 강조(타이머) 구현, 테스트 17개 통과, 화면 확인
- 연타 방지 근거 조사: Apple HIG·Material 3·WCAG 2.5.2·NN/g 2건 (결정은 사용자)
- 로컬 확인 서버의 1시간 상한이 맥 절전 중 동작하지 않아 3시간 55분 동안 켜져 있던 것을 발견 → 종료하고 벽시계 기준 상한으로 교체(5초 시험으로 확인)
- ⚠ 로그인 준비 때 테스트 기록과 함께 **로그 33줄을 백업 없이 지움**(사용자는 로그를 남기길 원함) → 9/24 원문에서 23줄 복원: [logs/](logs/README.md)

### 9/24
- 쓰지 않는 npm 파일(`node_modules/`·`package.json`·`package-lock.json`·`src/`)을 휴지통으로 옮김(사용자 지시)
- 조사: 쿠라스 원문 팁(10초는 모든 푸어, 「농도 조절」은 세 번째 푸어부터), 붓는 모양은 원문·영상 설명란에 없음, MDN 공유 가능 파일 형식(.json·.md 없음), 넷플릭스 방치 확인 기준, AI 웹디자인 플랫폼
- 구현: 붓기 안내, 누른 시각 판정, 뗄 때 실행·밀어서 취소·되돌리기, 방치 확인, 단계 설명 고르기·직접 쓰기, 서버 등록·스위치·서버 뺀 무게, 설문 순서, AI 공유(MD/JSON·묶는 중·파일 저장·공유창), 설정의 서버·공유 기본 형식
- 테스트 25개 통과(새 8개). 브라우저(폰 크기)에서 확인: 붓기 안내, 붓기 끝 표시, 밀어서 취소, 연타 뒤 되돌리기 두 번, 종료 되돌리기, 방치 확인 창·자동 종료, 판정 표시, 서버 뺀 무게, 설문 순서, 공유 화면, 1:1 비교(캡처를 사용자에게 보냄)
- 고친 것: 푸어 직후 남은 붓기 시간이 11초로 보이던 것, 되돌리기 버튼·막대 클래스 이름 충돌, 준비 화면에서 붓기 팁이 두 번 보이던 것
- Firebase 진단: Google 로그인 설정됨, Firestore 있음, 규칙은 기본값(모두 거절)이라 교체 필요
- 시험 데이터는 지우고 로그는 남김(93줄)
- 로그인·규칙 확인: 계정에 로그가 써지고 읽힘, 다른 uid 읽기는 permission-denied
- 조사: Figma MCP(쓰기·좌석·한도·교육용 플랜·베타 무료), 린싱 목적(Intelligentsia·Stumptown·Counter Culture), 그라인더 변환 두 사이트(데이터 성격·이용 조건), 블루보틀 푸어오버 원문(푸어마다 역할을 따로 적음)
- 로그인 뒤 기록이 사라졌다는 문의 확인: 11:45 에이전트가 시험 기록을 지움 → 11:52 로그인. 그사이 새 기록 없음(로그), 계정에도 기록 없음 → 버그 아님. 대신 「계정마다 한 번만 묻는」 옮기기 확인의 빈틈을 발견(위 답이 필요한 것)
- 선택 설명(칩 describe), 출처 작게, 한국어 줄바꿈, 그라인더 변환 링크·참고 µm 구현. 테스트 27개, 360px 캡처로 확인
- 계정 옮기기 배너(가짜 기록 1건으로 확인 뒤 지움), 글자 대비, 누름 44px 적용. 테스트 28개
- 로그인 판정 실측(auth.signin readyMs): 전 2,450·2,563·2,663ms → 후 792·778·756ms(새 탭 첫 연결은 2,001ms). 「계정 확인 중」 표시는 코드만 넣고 화면으로는 못 잡음(0.8초 사이라 도구로 캡처 불가)
- 설정의 로그인 표시를 버튼 하나로, Google 공식 모양 버튼, Debug Log 스위치, AI 공유 「이 기록만」 — 360px 캡처로 확인
- Figma: 커넥터 연결됨, whoami 결과 교육용 팀 관리자·좌석 View → 쓰기 불가
- 조사: WCAG 2.2·KWCAG 2.2·KRDS·Apple HIG·Material 3·NN/g — 현재 앱을 재 보니 옅은 글자 대비·테두리 대비·터치 크기·줄 간격이 기준 미달(위 답이 필요한 것)
- 단어 교체: 가수·린싱·커피 원액 무게·서버 총 무게·종료 상태(모두 내려감/강제 종료)·제때/일찍/늦게·저울 누적 목표·분쇄 크기 (Click)·원두 노트·느낀 향과 맛. 준비 목록은 헹굼 → 얼음 순서로. 테스트 26개 통과, 폰 폭 캡처로 네 화면 확인
- 설정 그라인더 칸에 언스페셜티 링크 두 곳, AI 공유 고정 안내를 칸 제목 아래로(공유 화면도 같게). 테스트 28개 통과, 360px 캡처로 두 화면 확인
- 조사: 언스페셜티 분쇄도 가이드·브루잉 레시피(목록·상세·진행 화면)·브루잉 가이드(커피 컴퍼스·수율 계산), 설명 문장 위치(GOV.UK)·설정 행 모양(Apple HIG·Material 3·Android)
- 언스페셜티 후보 1~7·9 구현(새 파일 core/compass.js·core/chart.js·ui/charts.js·ui/icons.js), 백업 B안, 보조 설명 스위치, 아이콘 탭, 입력칸 제목 진하게, 「null」 버그 두 곳(원두 노트 칸·탭을 숨기는 타이머 화면 — `replaceChildren` 이 null 을 글자로 넣음) 수정. 테스트 37개 통과, 360px 캡처 8장(홈·준비·보조 설명 끈 준비·타이머·결과·기록 상세·설정·원두)으로 확인
- 공개 전 개인정보 점검: 글자 파일 0건, pptx 슬라이드·속성 0건, 이미지 위치정보 없음, 보고서 PDF 본문 표지에 학번·이름(사용자 판단 몫)
- 원두와 물의 비율 항목 신설, 링크 버튼 통일, 단계 설명 문장 줄바꿈, 보조 설명 통째 줄바꿈. 테스트 37개 통과, 360px 캡처로 준비·타이머·결과·기록 상세 확인
- 원두량·뜨거운 물·비율 세 칸 연동(브라우저 실측: 비율 15 → 물 240g, 원두 +1g → 17g·1:15·물 255g·얼음 74g). 캡처 스크립트가 실패할 때 Chrome 을 남기던 문제 고침(스크래치 도구)
- 레시피 가져오기(프롬프트·검사·폼 수정·직접 입력·목록), 「null」 재발 방지(fill + 테스트). 테스트 42개 통과. 브라우저 값 확인: 설문 없는 준비 화면 null 0줄, 오류 섞인 붙여넣기 → 고칠 곳 1개 정확히·저장 막힘, 칸 고침 → 오류 0·미리 보기·저장 가능, 저장 → 준비 화면 목록에 나옴, 폼에 고쳐 읽은 값(15·93·45·210)

### 9/25

- 사용자 실사용 피드백 반영(새벽): 로그인 버튼, 분쇄 「실제 125 · 영점 −3 반영」, 얼음 차이 → 가수 추천, 추출 후 가수 전·후 총무게와 자동 가수·비율 추천(서버 무게 재기 처음부터 켜짐), 설문 [저장]·[공유] 두 버튼, 「추출 보기/숨김」, 원두 등록 개편(이름 자동·배전도 슬라이더·구매 무게·제조일·개봉일·남은 원두·소모), 타이머 대기 화면과 [시작], 원 안 저울 값, 종료 기준 문구
- 공유 오류 원인: 크롬 원본(`share_service_impl.cc`·안드로이드 `ShareServiceImpl.java`·`navigator_share.cc`)을 읽고, 헤드리스 크롬 실제 클릭으로 재현(.md·.json → canShare true, share → `NotAllowedError: Permission denied` / .txt → 공유창 대기)
- 조사: 배전도(강배전 = 다크, 일본식 8단계·SCA Agtron 8구간), 원두 소비기한(법정 기간 없이 로스터리가 정함 — 6개월~1년·12개월·2년 사례), 재팬 아이스(얼음은 식히고 묽히는 몫), 쿠라스 종료 «Approx. 2:10»
- 테스트 46개 통과(새 4개: 가수·비율, 원두 남은 양·이름·제조일 역산, 타이머 대기, 공유창 파일 이름). 390px 캡처와 값으로 확인: 홈(로그인 버튼·6g 알림), 준비(실제 109 · 영점 −3, 얼음 62g → 8g 가수 안내), 타이머 대기·붓는 중(원 안 「지금 12g」), 결과(가수 후 613 − 전 605 = 8g 자동, 1:14.3), 설문(추출 숨김·두 버튼), 공유 안내, 원두 목록·등록(이름 자동 「에티오피아 예가체프 첼바 G1 워시드」, 12oz = 340.2g), 소모로 바꾸기(로그·준비 목록에서 빠짐), 처음부터 끝까지 한 번(얼음 62 → 기록 가수 처음 값 8g, `brew.start` 에 readySec)
- 캡처 도중 한 번 「화면이 그려지지 않았다」: 앞선 타이머 시험이 캡처용 프로필에 진행 중 추출을 남겨 앱이 타이머로 돌려보낸 것(앱 동작은 맞음) → 시드 페이지가 저장소를 비우고 시작하게 고침(스크래치 도구)
- 이어서(9/25): 날짜 칸을 [−] 날짜 [+] 로, 새 원두 날짜 기본값 오늘, 원두 수정 화면 [소모로 바꾸기]·[사용 중으로 복구], 글자 크기·여백 역할별 통일(CSS 변수 + 테스트). 테스트 47개 통과. 390px 캡처와 값으로 확인: 새 원두 날짜 오늘·소비기한 1년 뒤, 개봉일 −2 → 7일 전, 소모 → 복구(로그 consumed/form · active/form), 설정·설문·기록 상세·레시피 화면 여백·글자 크기
- 지난 보고의 「분쇄·얼음」 캡처는 두 조각을 이어 붙인 이미지라 「참고 µm」 밑에 「물 온도」가 붙어 보였음(실제 화면 아님) — 사용자에게 알림
- 이어서(9/25): AI 공유 프롬프트(코드 상자·복사·고치기·저장), 슬라이더 칸 정렬(직접 그림), ± 칸 가운데·여백, 칸 제목 크게. 테스트 48개 통과. 확인: 슬라이더 6개 눈금 어긋남 최대 0.02px, ± 칸 좌우 여백 차 0px·입력칸 140px, 프롬프트 복사 — 헤드리스 크롬 기본(클립보드 막힘)에서는 글 고르기 대비책·로그 ok:false, 클립보드 권한을 준 실제 클릭에서는 클립보드 = 프롬프트·아이콘 ✓·로그 ok:true, 고쳐 저장·되돌리기 단추·로그
- 조사: 설치한 웹앱이 다른 앱의 공유를 받는 share_target 은 크롬 안드로이드 76+·PC 크롬 89+ 만, Safari(iOS) 없음(MDN browser-compat-data). Gemini·Claude·ChatGPT 모두 대화에서 내려받을 파일을 만들 수 있다는 기사(형식·요금제·앱별 차이 있음)
- 이어서(9/25 밤): 기록이 없을 때 안내(로그인 전·뒤), 설정 로그인 모양 통일, AI 제안 가져오기(형식·프롬프트·붙여넣기·파일·준비 화면 적용), 아래 탭 슬라이드. 테스트 52개 통과. 확인: 탭 슬라이드 실제 클릭 — 1칸 2장/280ms · 2칸 3장/340ms · 3칸 4장/380ms, 방향, 끝나면 덮개 0, 1/8 속도 가운데 장면. 기록 없음 로그인 전 문구·버튼, 로그인 뒤(저장소를 cloud 로 바꿔 흉내) 문구·계정·버튼 없음. AI 제안 붙여넣기(단위 붙은 값 고쳐 읽기 경고 1) → 저장 → 준비 화면 적용(다이얼 110·원두 16.5·물 150 그대로·92℃, 로그 source ai)
- 사용자가 공유 파일 이름 `-md.txt` 를 물음 → 크롬 허용 목록 때문이라고 설명(9/25 공유 오류 수정 때 에이전트가 정한 방법, 이름만 바꾸는 것은 사용자 선택)
- 이어서(9/25 밤 2): 공유 이름 괄호 표기, 레시피 원두 탭으로, 시각 요소 다섯, 끌어 넘기기(가장자리 제외), 타이머 크게·원 누름·스크롤·확대 막음·기기 크기 대응. 테스트 53개 통과. 확인(헤드리스 크롬 실제 입력): 타이머 다섯 크기 스크롤 0·원 누름 시작/다음 로그 via ring·원 밀어서 취소 로그, 끌기 — 왼쪽·오른쪽 넘김, 가장자리 20px·아래 끝 무시, 세로·짧은 끌기 제자리, 첫 탭 바깥 제자리. 끌기 실패 1건 발견·수정: touch-action 을 화면 칸에만 걸어 내용이 짧은 기록 화면 빈 곳에서 시작한 끌기를 브라우저가 가져감 → html 전체로. 목록 → 상세 전환 15%·50% 멈춤 캡처 — 처음엔 상세가 화면보다 길어 커지는 모습이 안 보여 전환 중 화면 높이로 자르게 고침. 준비 → 타이머 z-forward. 공유 (md)·(json).txt 크롬 통과

### 9/26

- 사용자 실사용 피드백: 방치 확인 기준, 누른 시각 사후 수정(시작 보정·단계별), 타이머 원 배치, 테이스팅 노트 이름·슬라이더·노트 채우기, 결과 추가 입력·추가 얼음·무게 칸 연동, 제안 문구 클릭 우선, 버튼 색, 원두 탭 네 칸·그라인더 아이콘
- 분쇄 측정 CSV(언스페셜티 Export CSV) 조사: 평균은 파일 이름 `particle-M1347.69-…`, ± 는 Accuracy 칸(141.31), 표준편차는 CSV 에 없음. 칸 분포로 맞춰 본 식 중 면적 가중(아래 경계) 1340.1/339.8 이 가장 가까움(사진 1347.69/352.18). Count Density 최소 1.1765% → 입자 85개. CSV 시안을 만들었다가 사용자 정정(«설명만») 으로 앱에서 뗌
- 사고 1건: 파이썬 `open(p,'w').write(s)` 에서 s 가 튜플이 되어 `app/js/ui/icons.js` 가 0바이트 → 마지막 커밋에서 되살리고 그 뒤 바뀐 아이콘을 다시 넣음(메모리 «파일 쓸 때 조용히 깨짐»에 기록)
- 테스트 56개 통과. 390px 등 실측: 타이머 다섯 크기 스크롤 0·원 282~400px, 방치 확인 창(58초) → 고치기 → 단계 시작 [0,40]·로그 backdated, 무게 연동(605+20=625, 후 630→가수 25, 전 600→가수 30, 얼음 넣은 뒤 650→얼음 20, 물 모두 270g), 시작 보정 +3 표 반영, 선택 안 함 → 가운데 → 보통, 노트 채움(자두·초콜릿), 네 칸, 버튼 색 rgb(239,228,216)/(154,129,105)
- 9/26 두 번째 묶음(사용자 결정·요청): 분쇄 측정 CSV·사진 구현(CSV 시안 되살림 + 사진 글자 인식), 네 칸 전환 A안, 동작 줄이기·View Transitions 없음 대신 움직임, 용어 통일, AI 공유 「선택 안 함」 빼기, 기록 여러 건 골라 공유, 다크 로스트 안내·보조 설명 연동, 배전도 0.5~10, 노트 ×, 디카페인, 로스터리 맛 지표, 가공방식 출처 표시. 메모가 원두끼리 이어진다는 건은 사용자 확인으로 착각(코드상 원두별 저장) — 고치지 않음
- 사진 인식 튜닝 실측(헤드리스 크롬, 같은 코드): 처음 14/25 → 칸 나누기를 단어 가운데 기준으로 · 폭 1800·이진화 끔 · 숫자 단어만 다시 읽기 · 카드 왼쪽 띠 · 못 읽으면 2400 → 25/25(모든 칸 두 번 읽어 같음), 0.5~1초
- 헤드리스 크롬 실제 클릭: 준비 화면 사진 올리기 → 확인 창 여섯 칸 정확·확인 필요 0 → [영점 반영값] → 표시값 123(영점 −3 → 반영값 120), CSV 는 그래프·메모 g1 정확, 디카페인 1클릭(클릭당 16µm) 112→113·원두 16→16.5, 길게 누르기 → 1건 → 2건 → 공유 `…-2brews.md`(선택 안 함·사진 dataURL 없음), 원두 저장 {roast 중배전, roastLevel 5.5, decaf, 맛 지표 10점 산미 7, 노트 × 로 꽃 지움}, 보조 설명 끔 → 다크 안내·디카페인 설명 숨김, 네 칸 겹침 층·동작 줄이기 겹쳐 바꾸기·View Transitions 없을 때 새 화면 움직임, 로그(prep.decafAssist·grind.measureImport·share.selectStart·share.build) 남음. 테스트 64개 통과
